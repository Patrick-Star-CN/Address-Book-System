#include "../include/userList.h"

int main() {
    UserList list;
    list.addNode();
    list.addNode();
    list.addNode();
    list.addNode();
    list.fetchNode();
    //list.display();
    system("pause");
    return 0;
}
